pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				bat 'echo deu certo'
			}
		}
		stage('Test') {
			steps {
				bat 'echo deu certo'
			}
		}
		stage('Deliver') {
			steps {
				bat 'echo deu certo'
			}
		}
	}
